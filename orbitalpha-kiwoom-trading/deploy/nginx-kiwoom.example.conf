# 예시: Nginx reverse proxy — /live/ → 127.0.0.1:3001 , /paper/ → 127.0.0.1:3002
# 실제 server_name / TLS / 인증서 경로는 환경에 맞게 수정하세요.
#
# 적용: sites-available 에 복사 후 symlink, nginx -t && systemctl reload nginx
#
# trailing slash: 아래 redirect 로 /live 와 /live/ 를 정리 (paper 동일)
#
# 앱 .env: KIWOOM_LIVE_PUBLIC_MOUNT=/live, KIWOOM_PAPER_PUBLIC_MOUNT=/paper (인증·리다이렉트·cross-nav-links).

# map $http_upgrade $connection_upgrade { default upgrade; '' close; }

# server {
#   listen 443 ssl http2;
#   server_name orbitalpha.kr;
#
#   ssl_certificate     /etc/letsencrypt/live/orbitalpha.kr/fullchain.pem;
#   ssl_certificate_key /etc/letsencrypt/live/orbitalpha.kr/privkey.pem;
#
#   location = /live {
#     return 301 /live/;
#   }
#   location = /paper {
#     return 301 /paper/;
#   }
#
#   # --- LIVE 계좌형 모니터 (민감) — 아래 "보안" 블록 참고 ---
#   location /live/ {
#     proxy_pass http://127.0.0.1:3001/;
#     proxy_http_version 1.1;
#     proxy_set_header Host              $host;
#     proxy_set_header X-Real-IP         $remote_addr;
#     proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
#     proxy_set_header X-Forwarded-Host  $host;
#     proxy_set_header X-Forwarded-Port  $server_port;
#     proxy_read_timeout 60s;
#   }
#
#   # --- PAPER 급등주 모의매매 (read-only UI) ---
#   location /paper/ {
#     proxy_pass http://127.0.0.1:3002/;
#     proxy_http_version 1.1;
#     proxy_set_header Host              $host;
#     proxy_set_header X-Real-IP         $remote_addr;
#     proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
#     proxy_set_header X-Forwarded-Host  $host;
#     proxy_set_header X-Forwarded-Port  $server_port;
#     proxy_read_timeout 60s;
#   }
# }

# ---------------------------------------------------------------------------
# 최소 보안 예시 (택 1 또는 조합) — live 경로에만 우선 적용 권장
# ---------------------------------------------------------------------------
#
# (A) IP 제한 (사무실/홈 고정 IP 등)
#   location /live/ {
#     allow 203.0.113.10;
#     allow 203.0.113.0/24;
#     deny all;
#     proxy_pass http://127.0.0.1:3001/;
#     ... 위와 동일한 proxy_set_header ...
#   }
#
# (B) Basic 인증
#   # htpasswd -c /etc/nginx/.htpasswd-kiwoom-live operator
#   location /live/ {
#     auth_basic           "Kiwoom live monitor";
#     auth_basic_user_file /etc/nginx/.htpasswd-kiwoom-live;
#     proxy_pass http://127.0.0.1:3001/;
#     ... proxy_set_header ...
#   }
#
# paper 는 read-only 이지만, 운영 정책에 따라 (A) 또는 별도 경로 제한을 검토하세요.
