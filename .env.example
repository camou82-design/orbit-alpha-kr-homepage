# Homepage admin (/admin-login, homepage_admin_auth cookie — JJ/실거래와 무관)
HOMEPAGE_ADMIN_PASSWORD=955104

# OpenAI (Blog Automation — 서버 전용)
OPENAI_API_KEY=

# 선택: 기본 모델 고정 (미설정 시 gpt-5.4-mini)
# OPENAI_BLOG_MODEL=gpt-5.4-mini

# orbitalpha-futures-paper (read-only dashboard at /futures-paper)
# Production (Vercel): fetch from Lightsail reader API — use the same secret on Vercel and Lightsail.
# ORBITALPHA_FUTURES_PAPER_API_URL=https://your-lightsail-or-domain.example.com
# ORBITALPHA_FUTURES_PAPER_API_SECRET=
# Local dev only: read disk directly (optional)
# ORBITALPHA_FUTURES_PAPER_ROOT=

# Contact 폼 (Resend — 서버 전용)
RESEND_API_KEY=
CONTACT_TO_EMAIL=
# 발신 주소(Resend에서 인증한 도메인 또는 테스트용 onboarding@resend.dev)
# CONTACT_FROM_EMAIL=OrbitAlpha <onboarding@resend.dev>
